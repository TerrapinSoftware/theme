theme:

title: 'Terrapin Publications'

defaults:
  -
    scope:
      path: ""      # an empty string here means all files in the project
      type: pages
    values:
      # allow site search to reach this page
      search: true
      # the group of the search
      search_group: default
      # do not verify logins
      verify: false
      # display a TOC on page
      toc: true
      # sidebar settings
      sidebar: 
        file: _sidebar.md
        width: 250

exclude:
  - README.md
